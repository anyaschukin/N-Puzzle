package solver

// look at current board for potential moves
// check if move is valid
// generate board for move
// ... how to check if board is duplicate?

func MovePieces(puzzle []int, size int) []int {
	new := make([]int, size*size)
	return new
}
